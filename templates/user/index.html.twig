{% extends 'base.html.twig' %}

{% block title %}Tous les utilisateurs!! {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {# <h1>Hello {{ controller_name }}! ✅</h1> #}
    <h1>Tous les utilisateurs!! ✅</h1>

    {# This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'D:/wamp/www/sortir/src/Controller/UserController.php'|file_link(0) }}">src/Controller/UserController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'D:/wamp/www/sortir/templates/user/create.html.twig'|file_link(0) }}">templates/user/index.html.twig</a></code></li>
    </ul>#}
    {% if is_granted('ROLE_ADMIN') %}
    <table class ="table table-bordered table-striped">
        <tr >
            <td>Pseudo</td>
            <td>Nom</td>

            <td>Action</td>
        </tr>


        {% for user in users %}
            <tr>
                <td>{{ user.pseudo }}</td><td>{{ user.prenom }} {{ user.nom }}</td>

                <td><a href="{{ url('user_delete', { 'id': user.id }) }}" title="Supprimer un utilisateur">Supprimer</a></td>

            </tr>

        {% endfor %}
    </table>
    {% endif %}
</div>
{% endblock %}
